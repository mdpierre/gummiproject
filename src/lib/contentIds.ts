export function topicContentId(id: string): string {
  return `topic:${id}`;
}

export function lessonContentId(id: string): string {
  return `lesson:${id}`;
}

export function storyContentId(id: string): string {
  return `story:${id}`;
}
